# v0.1 verification evidence

**Verified:** 2026-09-04  
**Scope:** Foundation, contracts, deterministic fake path, and corpus structure only

## Passing evidence

- `pnpm install --frozen-lockfile` completed from `pnpm-lock.yaml`.
- `uv sync --frozen --python 3.12.11` completed from `uv.lock`.
- `pnpm browser:install` installed the Playwright-managed Chromium build.
- `pnpm check:all` passed under Node.js 22.23.1:
  - exact toolchain checks, including a headless Chromium launch;
  - formatting and TypeScript/Python lint;
  - TypeScript build/type-check;
  - 11 unit tests;
  - 2 integration tests, including two-process deterministic CLI execution;
  - the v0.1 no-op engine benchmark.
- A documented manual `pnpm still-shift animate` invocation returned API version `0.1`,
  scene schema version `0.1`, 150 frames for five seconds at 30 FPS, and an explicitly
  non-video no-op artifact.

## Intentionally incomplete evidence

The repository contains no real explainer-project images. The corpus manifest therefore
has zero entries, `status: "incomplete"`, no freeze timestamp, and explicit outstanding
requirements. `pnpm corpus:check` correctly exits non-zero. The v0.1 release is blocked
on supplying and reviewing 30–50 real images with complete category, dimensions, rights,
duration, and checksum metadata.

No v0.2 depth preparation or later renderer/export implementation was started.
