# Local corpus media

Place the 30–50 reviewed real explainer-workflow images here when their rights permit
local use. This directory is ignored by Git. Record every image in
`../corpus-manifest.json`, including untracked/private paths, dimensions, rights, and
expected shot duration.

Do not mark the manifest `frozen` until all readiness checks pass.
